# Go2 web dashboard — pinned to the versions in ~/dimensional-applications/.venv
# (Python 3.12.14). Install with the venv active:
#   uv pip install -r requirements.txt
# unitree-webrtc-connect pulls in aiortc, aioice, av, requests, pycryptodome,
# numpy, opencv-python and the rest of its own dependency tree.

fastapi==0.141.1
starlette==1.6.0
uvicorn==0.53.0
websockets==16.1.1          # uvicorn needs a WebSocket backend for /ws/control
unitree-webrtc-connect==2.2.0
aiortc==1.15.0
aioice==0.10.2
av==17.1.0                  # video frames from the WebRTC track
pillow==12.3.0              # JPEG encoding for the camera stream
requests==2.34.2            # LAN signaling handshake (bounded in app.py)
cryptography==50.0.1
